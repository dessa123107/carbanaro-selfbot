const Discord = require("discord.js");

module.exports = {
    name: 'spell',
    description: "spells the word out",
    execute(message, args, client){
        
        
        message.channel.send('hi')
        return;

        
        
  
  
        
    }
}